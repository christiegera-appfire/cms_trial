# Multiselect (Scope module)

You can streamline your work by selecting and moving multiple tasks.

To select multiple tasks from the list:

1. Open the Scope module and locate the tasks you want to select.
2. Mac users:

   1. Press and hold the **Command** key **(⌘)**.
3. Windows users:

   1. Press and hold the **Control** key **(Ctrl)**.

To select multiple tasks listed together:

1. Click the first task.
2. Press and hold the **Shift** key.
3. Click the last task in the list to select all tasks in between.

![Screenshot of the multi-select feature in the Scope module.](/cms_trial/assets/071833ec-6c6d-4532-b73c-e6c43484b19e.png)

## Select and unselect tasks

The table below presents all the possible actions.

| **Action** | **Description** |
| --- | --- |
| Selecting more than one task is possible with (selected tasks are highlighted in blue): |
| Cmd or Ctrl + left mouse button | Select tasks one by one |
| Shift + left mouse button | Select a range of tasks |
| Unselecting tasks is possible with: |
| ESC | Unselect all selected tasks |
| Click out of the zone | Unselect all selected tasks |
| Cmd or Ctrl + left mouse button | Unselect tasks one by one |

## Possible actions

Since tasks on the list can be structured, not all operations are possible. The table below presents a detailed list of actions.

| **Selected tasks** | **Action** |
| --- | --- |
| All children from one parent, but without a parent | Outdent |
| Multiple parents without children | N/A |
| Parent + all his children | Move up/down, indent, outdent |
| Parent + selected children/without children | N/A |
| Selected children from different parents, but without parents | N/A |
| Selected children from a single parent, but without a parent | Move up/down, indent, outdent |