# Filter options

## Filter options (old navigation)

Click to expand the guide

## Filter options (tasks)

![filtering-options.png](/cms_trial/assets/0e2e4302-4dd2-460d-ba16-2241d574ebd3.png)

These options are available in the following modules:

| **Module** | **Show basic tasks (filters don't apply)** | **Show all children tasks in results** | **Hide parent tasks in the results** | **Apply filters to all modules** | **Clear all active filters** |
| --- | --- | --- | --- | --- | --- |
| Gantt | ✅ | ✅ | ✅ | ✅ | ✅ |
| Scope | ✅ | ✅ | ✅ | ✅ | ✅ |
| Board | ✖️ | ✖️ | ✅ | ✅ | ✅ |
| Objectives | n/a | n/a | n/a | n/a | n/a |
| Resources | ✅ | ✖️ | ✅ | ✅ | ✅ |
| Teams | n/a | n/a | n/a | n/a | n/a |
| Risks | ✖️ | ✖️ | ✖️ | ✅ | ✅ |
| Calendar | ✖️ | ✖️ | ✖️ | ✅ | ✅ |
| Reports | n/a | n/a | n/a | n/a | n/a |

### Show basic tasks (filters don't apply)

When the “Show basic tasks (filters don't apply)” option is selected, all the basic tasks are displayed —even those that don't match the filter criteria.

### Show all children tasks in results

When this checkbox is checked, the filtered tasks (the ones that meet filtering criteria) are displayed along with their child tasks and any lower-level ones (that do not meet filtering criteria).

### Hide parent tasks in the results

When parent tasks are **NOT** hidden, the results contain:

- the tasks that match the search conditions
- greyed out parent tasks of the matches

![no-hide-parents.png](/cms_trial/assets/fb36d2a2-15f5-4efc-a4bd-cd8cd469f21a.png)

When you select the "Hide parent tasks in results" checkbox, the parent tasks (greyed-out) are **NOT** displayed.

![hide-parents.png](/cms_trial/assets/d868dc93-4986-400f-adb1-6acd30c31432.png)

**Note**: Task tree structure changes (move up, move down, indent, outdent, drag-and-drop) are unavailable when the "Hide parent tasks in results" checkbox is selected.

### Apply active filters to all modules

Filters apply to the module you are in. Use this option to apply all the current filters to all available modules.

**Note:** The "Apply active filters to all modules" option applies the filters to all the modules, even those for which these filters can **NOT** be used directly. For example, dates can be applied in the Gantt module but not in the Scope module.

Filters are applied by module; therefore, there are two ways of clearing them once they were used to all modules by the "Apply active filters to all modules":

- [Clear all active filters in each module](#)
- Clear all active filters in a module you're in, and use "Apply active filters to all modules" to clear the filters everywhere else.

### Clear all active filters

This option clears all active filters in a current module.

## Filter options (boxes)

In the Overview module, the following **FILTER OPTIONS** are available:

![Filter options for Boxes](/cms_trial/assets/e9c5dca8-b697-4a12-bc44-adc7fd387378.png)

### Hide parent boxes

Availability:

- Hierarchy mode ✅
- Timeline mode ✅
- Kanban mode ✖️

When parent Boxes are **NOT** hidden, the results contain:

- Boxes that match the search conditions
- greyed out parent Boxes of the matches

![Rows are not hidden in the Overview module](/cms_trial/assets/f1931d43-4eba-4d22-945b-7a8eefef7d99.png)

When you select the “Hide parent boxes in results” checkbox, the parent boxes (greyed-out) are **NOT** displayed.

![Rows are not displayed in the Overview module](/cms_trial/assets/76d532c9-d53d-40dd-9aa1-20c3d0d24b0c.png)

### Clear all active filters

This option clears all active filters in a current module.

## Filter options (new navigation)

Click to expand the guide

## Filter options (tasks)

The table below presents the filter options and their availability in modules.

![Screenshot of the filter options available when using filters in the Gantt module.](/cms_trial/assets/46201460-d051-44ae-819d-0029350915ac.png)

| **Module** | **Show BigPicture tasks when using Jira filters** | **Show all children tasks in results** | **Hide parent tasks in the results** | **Apply active filters to all modules** |
| --- | --- | --- | --- | --- |
| Gantt | ✅ | ✅ | ✅ | ✅ |
| Scope | ✅ | ✅ | ✅ | ✅ |
| Board | ✖️ | ✖️ | ✅ | ✅ |
| Objectives | n/a | n/a | n/a | n/a |
| Resources | ✅ | ✖️ | ✅ | ✅ |
| Teams | n/a | n/a | n/a | n/a |
| Risks | ✖️ | ✖️ | ✖️ | ✅ |
| Calendar | ✖️ | ✖️ | ✖️ | ✅ |
| Reports | n/a | n/a | n/a | n/a |

### Show BigPicture tasks when using Jira filters

When the **Show BigPicture tasks when using Jira filters** option is selected, all BigPicture tasks are displayed, even those that don't match the filter criteria.

### Show all children tasks in results

When this checkbox is checked, the filtered tasks (those that meet the filtering criteria) are displayed along with their child tasks and any lower-level tasks (that do not meet the filtering criteria).

![Video presenting hoe to use the Show all children tasks in result filter option in the Gantt module.](/cms_trial/assets/e5dbe98f-a13a-4658-bbdd-ec3cc5b39b0e.mp4)

### Hide parent tasks in the results

When parent tasks are **NOT** hidden, the results contain:

- the tasks that match the search conditions
- greyed out parent tasks of the matches

![Screenshot of using the search box when the Hide parent tasks in results option is disabled.](/cms_trial/assets/11b0af8f-1a66-478a-80e1-ca231916e47c.png)

When you select the **Hide parent tasks in results** checkbox, the parent tasks (greyed-out) are **NOT** displayed.

![Screenshot of using the search box when the Hide parent tasks in results option is enabled.](/cms_trial/assets/6802e3dc-5f8b-4e76-ba2d-1217416070e5.png)

**Note**: Task tree structure changes (move up, move down, indent, outdent, drag-and-drop) are unavailable when the **Hide parent tasks in results** checkbox is selected.

### Apply active filters to all modules

Filters apply to the module you are in. Use this option to apply all the current filters to all available modules.

**Note:** The **Apply active filters to all modules** option applies the filters to all the modules, even those for which these filters can **NOT** be used directly. For example, dates can be applied in the Gantt module but not in the Scope module.

Filters are applied by module; therefore, there are two ways of clearing them once they have been used to all modules by the **Apply active filters to all modules** option:

- Clear all active filters in each module
- Clear all active filters in the current module, and use **Apply active filters to all modules** to clear the filters across all other modules.

### Clear all active filters

This option clears all active filters in the current module.

## Filter options (boxes)

In the Overview module, the following **FILTER OPTIONS** are available:

![Screenshot of the Filter options in the Overview module.](/cms_trial/assets/ecbd31d1-f3f1-4c5f-9c44-b8b8b17c6894.png)

### Hide parent boxes in results

Availability:

- Hierarchy mode ✅
- Timeline mode ✅
- Kanban mode ✖️

When parent boxes are **NOT** hidden, the results contain:

- boxes that match the search conditions
- greyed out parent boxes of the matches

![Screenshot of using the serach box when the Hide parent boxes in results option in disabled in the Overview module.](/cms_trial/assets/cb3362ae-9b50-4911-b413-2ecb5e85b704.png)

When you select the **Hide parent boxes in results** checkbox, the parent boxes (greyed-out) are **NOT** displayed.

![Screenshot of using the serach box when the Hide parent boxes in results option in enabled in the Overview module.](/cms_trial/assets/8432417a-45d5-46aa-9f9d-6170a4b3343f.png)

### Clear all active filters

This option clears all active filters in a current module.