# Use case: Build a task structure with basic tasks / BigPicture tasks

Basic tasks have been renamed to **BigPicture tasks** as part of the new navigation rollout.

Both names refer to the same functionality. You may see either term depending on whether you’re using the previous navigation or the new navigation that is currently being rolled out in BigPicture.

## Build a task structure with basic tasks (old navigation)

Click to expand the guide

|  |  |
| --- | --- |
| **Goal** | **Create extra levels of structure with basic tasks without affecting your Jira space.**  Create additional hierarchy levels using **basic tasks** in BigPicture and quickly organize data independently of Jira, with the flexibility to convert these tasks into Jira work items as needed. |
| **Scenario** | A project manager manages a complex project with many Jira work items. They find the Jira task hierarchy somewhat limiting when visualizing the different stages. To make the task structure more manageable, they use BigPicture's **basic tasks**, which exist only in BigPicture and are not synced with Jira. Basic tasks serve as high-level placeholders to categorize the existing Jira tasks better.  When needed, the project manager converts some of these basic tasks into real Jira work items directly from the BigPicture interface. |
| **Key benefits** | - **Simplified organization** - create extra structure levels without modifying the actual Jira space. - **Flexible planning** - test ideas or restructure your plan without impacting your Jira backlog. - **Quick conversion** - convert basic tasks into Jira work items when you need. - **Visual clarity** - improve navigation across complex projects by grouping them. |

## Preconditions

Role You need at least a Box Editor role to add basic tasks.

box The box needs to be populated with a Jira space.

## Build a task structure with basic tasks step-by-step

### Add basic tasks

With basic tasks, you can simulate extra levels in the structure of your Jira tasks. It’s a quick way to organize data. Basic tasks are stored only by BigPicture and are not synchronized with Jira.

1. Open a box in the Gantt or Scope module.
2. Click the **+**(plus icon) > **Create task**> **Basic task**.
3. Fill in the task information. It is not mandatory to add the Start date and End date.
4. Click **Create**. A newly added basic task is highlighted in the task structure.
5. To move a task higher in the hierarchy, **right-click on the task** > **Position** > **Move to top** or **Move up**.

The video presents how to create and move a basic task in the structure.

![Video presenting how to create and move a basic task in the structure.](/cms_trial/assets/f253cde9-d361-4510-a0f6-c097dff6adf3.mp4)

### Convert basic tasks to Jira work items

You can convert a basic task into a real Jira work item at any time.

1. Find a basic task to convert.
2. Right-click on a basic task and select **Convert to Jira issue**.
3. Select a Jira space and a work item type.
4. Click **Next**.
5. Complete details and click **Create**.

The video presents how to convert a basic task into a Jira work item.

![convert-to-jira-work-item.mp4](/cms_trial/assets/43a17d07-769c-4f31-b4e1-8c68143d64fb.mp4)

**Tip:** You can use a basic task as a parent for other tasks and aggregate data from nested tasks.

![image-20250509-095325.png](/cms_trial/assets/296d0051-017b-4ffe-a72d-246caec32bac.png)

## Expected outcomes

- Basic tasks are visually distinguishable from Jira work items.
- A task structure with additional levels and placeholders created using basic tasks improves project clarity without modifying the actual Jira space.
- When necessary, selected basic tasks can be converted into Jira work items.
- Project managers get greater flexibility and visual control over task planning and structure.

## Additional resources

- [Basic tasks / BigPicture tasks](/cms_trial/space/SPM/1918405930/Basic+tasks+%2F+BigPicture+tasks/)

## Build a task structure with BigPicture tasks (new navigation)

Click to expand the guide

|  |  |
| --- | --- |
| **Goal** | **Create extra levels of structure with BigPicture tasks without affecting your Jira space.**  Create additional hierarchy levels using **BigPicture tasks** and quickly organize data independently of Jira, with the flexibility to convert these tasks into Jira work items as needed. |
| **Scenario** | A project manager manages a complex project with many Jira work items. They find the Jira task hierarchy somewhat limiting when visualizing the different stages. To make the task structure more manageable, they use BigPicture tasks, which exist only in BigPicture and are not synced with Jira. BigPicture tasks serve as high-level placeholders to categorize the existing Jira tasks better.  When needed, the project manager converts some of these tasks into real Jira work items directly from the BigPicture interface. |
| **Key benefits** | - **Simplified organization** - create extra structure levels without modifying the actual Jira space. - **Flexible planning** - test ideas or restructure your plan without impacting your Jira backlog. - **Quick conversion** - convert BigPicture tasks into Jira work items when you need. - **Visual clarity** - improve navigation across complex projects by grouping them. |

## Preconditions

Role You need at least a Box Editor role to add BigPicture tasks.

box The box needs to be populated with a Jira space.

## Build a task structure with BigPicture tasks step-by-step

### Add BigPicture tasks

With BigPicture tasks, you can simulate extra levels in the structure of your Jira tasks. It’s a quick way to organize data. BigPicture tasks are stored only by BigPicture and are not synchronized with Jira.

1. Open a box in the Gantt or Scope module.
2. Click **Tasks** > **Create**> **BigPicture task**.
3. Fill in the task information. It is not mandatory to add the Start date and End date.
4. Click **Create BigPicture task**. A newly added task is highlighted in the task structure.
5. To move a task higher in the hierarchy, **right-click on the task** > **Position** > **Move to top** or **Move up**.

The video presents how to create and move a BigPicture task in the structure.

![Video presenting how to create a BigPicture task and move it in the task structure.](/cms_trial/assets/36801fa6-fc12-489e-9b4a-0e4a6beb51a4.mp4)

### Convert BigPicture tasks to Jira work items

You can convert a BigPicture task into a real Jira work item at any time.

1. Find a BigPicture task to convert.
2. Right-click on a task and select **Convert to Jira issue**.
3. Select a Jira space and a work item type.
4. Click **Next**.
5. Complete details and click **Create**.

The video presents how to convert a BigPicture task into a Jira work item.

![Video presenting hot to convert a BigPicture task to a Jira epic.](/cms_trial/assets/a5f61915-d17e-4265-b135-ac5df765dc34.mp4)

**Tip:** You can use a BigPicture task as a parent for other tasks and aggregate data from nested tasks.

![Screenshot of the BigPicture task used for aggregating data for nested Jira work items.](/cms_trial/assets/5291874d-be28-433a-91be-934ed6d491a6.png)

## Expected outcomes

- BigPicture tasks are visually distinguishable from Jira work items.
- A task structure with additional levels and placeholders created using BigPicture tasks improves project clarity without modifying the actual Jira space.
- When necessary, selected BigPicture tasks can be converted into Jira work items.
- Project managers get greater flexibility and visual control over task planning and structure.

## Additional resources

- [Basic tasks / BigPicture tasks](/cms_trial/space/SPM/1918405930/Basic+tasks+%2F+BigPicture+tasks/)