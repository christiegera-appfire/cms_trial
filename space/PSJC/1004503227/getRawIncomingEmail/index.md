# getRawIncomingEmail

This routine still exists for compatibility reasons. However, the cloud version is just an alias to the [getIncomingEmail](/cms_trial/space/PSJC/1002045477/getIncomingEmail/) because you may now access not only the raw body, but the headers and embedded messages as well.